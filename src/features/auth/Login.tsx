import React from 'react';
import './Login.scss';
import LoginForm from './LoginForm';

const Login = () => {
    return (
        <div className="login-page">
            <LoginForm />
        </div>
    );
};

export default Login;
